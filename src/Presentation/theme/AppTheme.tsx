import { StyleSheet } from "react-native";

export const MyColors = {
    primary: '#18EC38',
    background: '#FBF6EE'
}

export const MyStyles = StyleSheet.create({
    loading: {
        position: 'absolute',
        bottom: 0,
        top: 0,
        right: 0,
        left: 0
    }
});